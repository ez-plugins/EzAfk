<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM-4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.gyvex</groupId>
    <artifactId>ezafk-parent</artifactId>
    <version>3.1.1</version>
    <packaging>pom</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <adventure.scope>provided</adventure.scope>
        <maven.compiler.release>21</maven.compiler.release>
        <maven.surefire.plugin.version>3.5.5</maven.surefire.plugin.version>
        <paper.version>26.1.2.build.57-stable</paper.version>
        <bstats.version>3.1.0</bstats.version>
        <worldguard.version>7.0.13</worldguard.version>
        <jetbrains.annotations.version>26.0.2-1</jetbrains.annotations.version>
        <errorprone.annotations.version>2.41.0</errorprone.annotations.version>
        <log4j.api.version>2.25.2</log4j.api.version>
        <maven.compiler.plugin.version>3.15.0</maven.compiler.plugin.version>
        <maven.shade.plugin.version>3.6.2</maven.shade.plugin.version>
        <jaloquent.version>1.3.2</jaloquent.version>
        <!-- GitHub repository used for publishing to GitHub Packages -->
        <github.repository>ez-plugins/ezafk</github.repository>
        <!-- All test sources require MockBukkit. Skip test compilation (and
             execution) by default; the with-mockbukkit profile re-enables it. -->
        <maven.test.skip>true</maven.test.skip>
    </properties>

    <modules>
        <module>ezafk-api</module>
        <module>ezafk-core</module>
        <module>ezafk-paper-26.1</module>
        <module>ezafk-folia</module>
        <module>ezafk</module>
    </modules>

    <repositories>
        <repository>
            <id>papermc-repo</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        <repository>
            <id>central</id>
            <name>Maven Central</name>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
        <repository>
            <id>sk89q-repo</id>
            <url>https://maven.enginehub.org/repo/</url>
        </repository>
        <!-- JitPack -->
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
        </repository>
        <repository>
            <id>maxhenkel-public</id>
            <url>https://maven.maxhenkel.de/repository/public</url>
        </repository>
</repositories>

    <profiles>
        <!--
            Activated automatically when -Dmockbukkit.version=<ver> is passed (e.g. in CI).
            Kept separate because MockBukkit is a locally-built artifact not available in
            any public Maven repository; including it in the default dependency set would
            break 'mvn package' on machines that have not pre-built MockBukkit.
        -->
        <profile>
            <id>with-mockbukkit</id>
            <activation>
                <property>
                    <name>mockbukkit.version</name>
                </property>
            </activation>
            <properties>
                <!-- Re-enable test compilation and execution when MockBukkit is present. -->
                <maven.test.skip>false</maven.test.skip>
            </properties>
            <dependencies>
                <dependency>
                    <groupId>org.mockbukkit.mockbukkit</groupId>
                    <artifactId>mockbukkit-v26.1.2</artifactId>
                    <version>${mockbukkit.version}</version>
                    <scope>test</scope>
                </dependency>
                <dependency>
                    <groupId>org.testcontainers</groupId>
                    <artifactId>junit-jupiter</artifactId>
                    <version>1.20.1</version>
                    <scope>test</scope>
                </dependency>
                <dependency>
                    <groupId>org.testcontainers</groupId>
                    <artifactId>mysql</artifactId>
                    <version>1.20.1</version>
                    <scope>test</scope>
                </dependency>
                <dependency>
                    <groupId>com.mysql</groupId>
                    <artifactId>mysql-connector-j</artifactId>
                    <version>9.0.0</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>
</project>
